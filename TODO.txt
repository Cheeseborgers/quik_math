
# Renderer --------------------------------
add opengl version check based on build config or platform-specific checks / settings (constexpr int OpenGLVersion = 450;)
utilize above version check for renderer / window initialization and opengl function loading

# General --------------------------------
fic narrowing on input

# Textures --------------------------------
fix setting data and params for all texture creation
check texture filtering setting logic

# Playing state -----------------------------------
Fix pause bugs
remove crosshair